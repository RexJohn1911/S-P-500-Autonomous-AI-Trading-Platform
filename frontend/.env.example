# API Base URL for Web Dashboard
# When running Vite in dev mode (proxied to backend) or containerized same-origin, default is /api/dashboard.
# Set explicitly if frontend is hosted on a separate origin:
# VITE_API_BASE_URL=http://localhost:8000/api/dashboard
VITE_API_BASE_URL=/api/dashboard
